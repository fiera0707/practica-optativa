

apiVersion: v1
kind: Service
metadata:
  name: practica-optativa-service
spec:
  type: NodePort
  selector:
    app: practica-optativa
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30007